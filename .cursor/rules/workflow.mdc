---
description: Lite 开发发版流程。做功能、修 bug、发快照、发稳定时必须遵循。
alwaysApply: true
---

# Lite 流程

口令：做功能 / 修 bug / 发快照 / 发稳定或发 x.y.z / 同版本再发 / 发 Agent。未说发版就只改代码。

本地开发禁止 `git push`，`main` 和 `Snapshot` 都要明确说了才能推。没明确表示：修改和验证一律只在本地。除非明确说了 `上传`、`发版`、`发快照`、`发稳定`、`发 Agent`、`同版本再发` 或 `上传发版`。只说合某一个 PR，就只做那一件。`暂不发版` = 停。push `Snapshot` 会自动打快照 Release。

仓库：`Lite`（服务端）、`Lite-web`（系统 UI）、`Lite-agent`（节点）、`Lite-theme`（公共主题）。远端：https://github.com/nuomiiiii/Lite 、https://github.com/nuomiiiii/Lite-web 、https://github.com/nuomiiiii/Lite-agent 、https://github.com/nuomiiiii/Lite-theme 。远端是 `main`、`Snapshot`，没有 `dev`。Agent 只有 `main`，push `main` 就会发 Agent 快照。

开工：`git fetch`。合入 `origin/main` 上 bot 的 BGP 提交。冲突时默认保留远端 `database/tasks/return_route_bgp_prefixes.json`，禁止 force push 抹掉 bot。

# 验证

能在管理后台网页上完成的模拟、验收和回归，必须在本地 UI 里点，不要只打 API。网页才是用户直观使用的地方。同时检查文案、布局、交互和控件状态；发现问题记下来或当场改。

交本地二进制或镜像前：先 `Lite-web` `npm run build`，拷进 `web/public/systemUI`，再编本仓。GitHub 正式版和快照由工作流从 Lite-web 源码注入 system UI，不要把 `web/public/systemUI/dist` 提交进仓。Vite `5273` 热更新不等于已嵌入。生产路径禁止预览/模拟数据。

# 发版

快照：不改 x.y.z，也不改 `main`。远端没有 `Snapshot` 时，先从当前 `main` 新建 `Snapshot`，再在该分支上改；远端已有 `Snapshot` 时，直接在 `Snapshot` 上改。有 UI 改动时先推 web 的 `Snapshot`，再推 Lite 的 `Snapshot`（服务端 snapshot 构建固定 clone web 的 Snapshot 分支）。**快照不要给 Lite-web 打 GitHub Release、tag 或 `dist-release.zip`。** tag、GitHub Release 显示名、嵌入版本都是 `Snapshot-YYMMDDHHMM`（北京时间），例如 `Snapshot-2608162321`。禁止标题写成「Lite 2.2.3 Snapshot」。Release 正文只写本次变更，不要把历史快照说明整份贴进去。产物：Linux 五架构 + SHA256SUMS + lite-update.json；Docker 只打当前仓 GHCR 的 `:snapshot`（`ghcr.io/nuomiiiii/lite:snapshot`）；无 Windows。prerelease，CI 会删旧 Snapshot-*。正式更新提示不认快照。

稳定新版本：bump `utils/version.go` 与 web `package.json`，合进 `main`。先发 `Lite-web` 正式版（把 `x.y.z` tag 移到 web `main`，更新 Release 说明；`dist-release.zip` 由 web 工作流上传），再跑 Lite `publish-stable-release.yml`，不传 `version_hash`，也不传 `web_ref`（工作流用本次版本号去 clone 同号 Lite-web tag，并核对 `package.json` 版本）。标题等于 `x.y.z`。产物含 Windows。Docker `:x.y.z` 与 `:latest`。不要恢复已删的 `follow-komari-release.yaml` / `build.yaml`。

同版本再发：有 UI 改动时同样先发 web（移 tag、更新说明，zip 由工作流重打），再走 Lite `rebuild-release.yml`，必须换新的七位 `lite-version-hash`，`frontend_ref` 同样留空。禁止对已有 tag 再跑空 hash 的 publish-stable-release（会复用旧号，用户收不到更新）。构建号以发版时嵌入的七位 hash 为准。

Agent 四段号，协议没变不要空发。`2.3.0.0` 已原生适配 Lite。一键安装走 `nuomiiiii/Lite-agent` 最新脚本和 `ghcr.io/nuomiiiii/Lite-agent:latest`，不要锁死版本号。Komari 协议头仍兼容更旧 Agent。

# CI

发版只认现有文件：`publish-stable-release.yml`、`rebuild-release.yml`、`release.yml`、`snapshot.yml`。本地和 GitHub 都已删除、不要找回：`docker-publish.yml`（旧实验，push main 已关）、`test-sqlite-v4-docker.yml`（只绑已不存在的 `test-2.1.7-v4`）、`development.yml`、`auto-merge-dev-to-main.yml`。没有 `dev`、`candidate/*`。Docker 正式产物由 `release.yml` / `snapshot.yml` 打，不经过上述已删文件。

clone `Lite-web` 和 `Lite-theme` 的 URL 不要写死 owner。未填输入时按当前仓的 `github.repository_owner` 拼兄弟仓。当前远端是 https://github.com/nuomiiiii/Lite-web 与 https://github.com/nuomiiiii/Lite-theme 。正式版、同版本再发和快照都走 `build-frontend`：编最新 Lite-theme 写入 `bundledThemes/Lite-theme` 和 `rescueTheme`，不要只检查仓里已有的旧主题。未填 `theme_ref` 时用 Lite-theme `main`（主题仓没有 Snapshot 分支）。

# 硬限制

- GitHub 文本必须 LF，不要 CRLF。不要用 PowerShell Set-Content 写仓库文件。
- commit / PR / Release / 注释禁止任何 AI、Cursor、Generated by、Co-authored-by 标记。
- 禁止把本地相关内容带到 GitHub：调试记录、验证记录、测试环境数据不得出现在 commit、PR、Release 说明或仓库文件里。包括本地库、日志、截图、账号口令、端口与路径、`runtime/` 产物、测试机配置、真实 token。Release 只写面向用户的产品变更。
- 功能和修 bug 按 Geekbench 5 单核约 600 验收。1 核热路径。沿用 SQLite WAL、按 CPU/内存限并发、V4/V6 压缩、分层 rollup。禁止请求路径 VACUUM、时序写入配置库、未压缩大 blob、让 metrics.db 膨胀。
- 不 force push `main` / `Snapshot`。未完成功能不进稳定。
